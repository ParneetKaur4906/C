#include <stdio.h>

int main(){
    printf("Welcome\vTo\vChitkara\vUniversity");
    return 0;
}
// This program demonstrates the use of escape sequences in C.
// It uses vertical tab escape sequences (\v) to format the output by inserting vertical spaces between lines.
// The program prints "Welcome", "To", and "Chitkara University" on separate lines with vertical spacing.
// The program uses the C17 standard for compilation.